# Tests

Developer tests for `Scripts/SSF2/INSTALL_SSF2.sh`. Not intended for end users.

Run from any directory:
```bash
bash Scripts/SSF2/Tests/test_download_fallback.sh
```

Tests use mocked `wget` - no network calls, no downloads.
